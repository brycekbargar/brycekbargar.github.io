<!DOCTYPE>
<html>
	<head>
		<meta charset="utf-8">
		<title>{{ site.name }}</title>
	</head>
	<body>
		{{#each all }}
		<div>
			<a href="{{ path }}">{{ title }}</a>
			<div>{{ date }}</div>
			<div>{{ blurb }}</div>
		</div>
		{{/each }}
	</body>
</html>
