<!DOCTYPE>
<html>
	<head>
		<meta charset="utf-8">
		<title>{{ site.name }}</title>
	</head>
	<body>
		<div>{{{ contents }}}</div>

		{{#each life }}
		<div>
			<a href="{{ path }}">{{ title }}</a>
			<div>{{ date }}</div>
			<div>{{ blurb }}</div>
		</div>
		{{/each }}
	</body>
</html>
