<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>
      {{ site.title }}{% if title %}: {{ title }}{% endif %} {% if subtitle %} -
      {{ subtitle }}{% endif %}
    </title>

    <link rel="stylesheet" href="/static/site.css" />
    {% rendersection "styles" %}
    <!---->
    {% rendersection "preloads" %}
  </head>

  <body>
    <main>
      {{ content | safe }}
    </main>
    <div
      role="status"
      class="sr-only"
      x-data
      x-text="$store.live.message"
      :aria-live="$store.live.level"
    ></div>
    <script>
      document.addEventListener("alpine:init", () => {
        Alpine.store("live", {
          message: "",
          level: "polite",
          announce(message) {
            this.message = message;
            this.level = "polite";
          },
          assert(message) {
            this.message = message;
            this.level = "assertive";
          },
        });
      });
    </script>
    {% rendersection "scripts" %}
    <!---->
    {% if site.environment === 'production' %}
    <script
      defer
      src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js"
    ></script>
    {% else %}
    <script defer src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.js"></script>
    {% endif %}
  </body>
</html>
